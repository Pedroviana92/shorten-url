spring.application.name=shorten-url

# Application Configuration
app.base-url=http://localhost:8080/
app.hashids.salt=my-secret-salt-2026
app.hashids.min-length=6

# Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/url_shortener_db
spring.datasource.username=url_app_user
spring.datasource.password=992900kkPP*
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Cache Configuration
app.cache.ttl-seconds=30
app.cache.key-prefix=urlshortener:

# Logging for debugging cache
logging.level.com.shorten.url.shortenUrl.services.impl.CacheServiceImpl=DEBUG